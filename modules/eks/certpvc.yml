apiVersion: v1
kind: PersistanceVolumeClaim
metadata:
  name: cert-pvc
spec: 
  storageClassName:
  accessModes:
    - ReadWriteOany
  resources:
    requests:
      storage: 5Gi
  storageClassName: pj-sc