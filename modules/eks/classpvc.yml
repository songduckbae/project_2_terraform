apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: class-pvc
spec: 
  storageClassName:
  accessModes:
    - ReadWriteOany
  resources:
    requests:
      storage: 5Gi
  storageClassName: pj-sc